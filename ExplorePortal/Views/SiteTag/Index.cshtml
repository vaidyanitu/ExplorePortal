@model IEnumerable<ExploreModel.SiteTagModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SiteId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TagId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SiteId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TagId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SiteTagId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SiteTagId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SiteTagId })
        </td>
    </tr>
}

</table>
